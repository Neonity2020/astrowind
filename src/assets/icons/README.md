# Local SVG icons

Drop `.svg` files here and use them as `<Icon name="file-name" />` (`astro-icon`, `iconDir` in `astro.config.ts`).
The directory must exist, otherwise astro-icon warns on every build.
